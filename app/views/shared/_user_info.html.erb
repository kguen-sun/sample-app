<%= link_to gravatar_for(current_user, size: Settings.image.gravatar.index_size) %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.size, t(".micropost") %></span>
